export const RATE_LIMIT_WINDOW = 5 * 60 * 1000; // 15 minutes
export const RATE_LIMIT_MAX = 100; // limit each IP to 100 requests per windowMs
export const ENV_KEYS = ['NODE_ENV', 'SERVER_PORT', 'SERVER_HOST', 'CORS_ORIGIN_URL']; // The keys of the env that are to be used in the project
export const BASE_PATH = '/api/';
export const VERSION = '1.0.0';
export const DESCRIPTION = 'This project is for listing the films.';
